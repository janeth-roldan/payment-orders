# ============================================
# Docker Profile Configuration
# ============================================
# Este perfil se activa cuando SPRING_PROFILES_ACTIVE=docker
# Sobrescribe solo las configuraciones específicas de Docker

# R2DBC Configuration - Usa el nombre del servicio Docker como host
spring.r2dbc.url=${DB_URL:r2dbc:postgresql://postgres:5432/paymentorders}

# Logging - Menos verbose en Docker
logging.level.com.banking.paymentorders=INFO
logging.level.org.springframework.r2dbc=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
